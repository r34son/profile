spec:
  containers:
    - image: ${image_url}
      name: app
      command: ['node', 'server.js', '>', '/dev/ttyS1']
      volumeMounts:
        - mountPath: /dev/ttyS1
          name: log-port
  restartPolicy: Always
  volumes:
    - name: log-port
      hostPath:
        path: /dev/ttyS1
